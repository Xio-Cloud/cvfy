name: Build Preview Deployment
on:
  pull_request:
    types: [opened, synchronize]

# cancel in-progress runs on new commits to same PR (github.event.number)
concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.sha }}
  cancel-in-progress: true

jobs:
  build-preview:
    runs-on: ubuntu-22.04
    name: Build Preview Site and Upload Build Artifact
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build dist folder
        run: |
          npm run generate || true
          rm -rf dist
          mkdir -p dist
          cp -r .nuxt/dist/client/* dist/ 2>/dev/null || true
          cp -r public/* dist/ 2>/dev/null || true
          cp -r .output/public/* dist/ 2>/dev/null || true
          if [ ! -f dist/index.html ]; then
            echo "Error: Static site build failed - dist/index.html not generated!"
            exit 1
          fi

      # Uploads the build directory as a workflow artifact
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: preview-build
          path: ./dist
